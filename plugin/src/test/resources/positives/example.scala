// Example of how a test case can be done

package localhost

class example {
  implicit def f(x: Int) = "provoke feature warning"
}

// warning: there were 1 feature warning(s); re-run with -feature for details
//
